CompiledCamphorScript(略してComCamScr)の仕様
文は次の8種類
var 識別子;
変数宣言。

delete 識別子;
変数削除。変数の値は0である必要あり。

read(識別子);
入力を変数に格納。

write(識別子);
変数を出力。

while(識別子){0個以上の文}
変数が0でない限りループ。中でvarを書いたらdeleteが必要。
}の直前の;は省略可能。

識別子+=定数;
加える。

識別子-=定数;
引く。

;
空文。