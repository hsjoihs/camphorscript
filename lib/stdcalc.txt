#define c_ constant char

/* a=0; */
#def clear(char& a) 
	while(a){a-=1;}
#end_def




#def (=)(char& a;c_ N)
	clear(a);a+=N;
#end_def


#include <plus_eq_til>
#include <minus_eq_til>
#include <eq_til>



#def (+=)(char& a;char& b)
	char c;
	(a,c)+= ~b;
	b+= ~c;
	delete c;
#end_def

#def (-=|~)(char& a;char& b)
	/* if(b>=a){b=0;a=0;}else{a-=b;b=0;} */
	char c;char d;
	while(b)
	{
		(c,d)+= ~a;
		   a += ~d;
		while(c){
			clear(c);
			a-=1;
		}
		b-=1;
	}
	delete c;delete d;
#end_def

#undef c_