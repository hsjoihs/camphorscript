
mov 1; inc 9; 
mov 1; loop; mov 0; inc 11; 
	mov 1; dec 1; 
mov 1; pool; assert_zero 1; 
mov 0; loop; mov 1; loop; mov 1; dec 1; 
	mov 1; pool; assert_zero 1; 
	mov 2; loop; mov 2; dec 1; 
	mov 2; pool; assert_zero 2; 
	mov 0; loop; mov 1; inc 1; 
		mov 2; inc 1; 
		mov 0; dec 1; 
	mov 0; pool; assert_zero 0; 
	mov 2; loop; mov 0; inc 1; 
		mov 2; dec 1; 
	mov 2; pool; assert_zero 2; 
	mov 5; loop; mov 5; dec 1; 
	mov 5; pool; assert_zero 5; 
	mov 2; inc 9; 
	mov 1; loop; mov 4; inc 1; 
		mov 2; loop; mov 3; inc 1; 
			mov 4; loop; mov 4; dec 1; 
			mov 4; pool; assert_zero 4; 
			mov 2; dec 1; 
		mov 2; pool; assert_zero 2; 
		mov 3; loop; mov 2; inc 1; 
			mov 3; dec 1; 
		mov 3; pool; assert_zero 3; 
		mov 4; loop; mov 2; inc 10; 
			mov 5; inc 1; 
			mov 4; dec 1; 
		mov 4; pool; assert_zero 4; 
		mov 2; dec 1; 
		mov 1; dec 1; 
	mov 1; pool; assert_zero 1; 
	mov 1; inc 9; 
	mov 2; loop; mov 1; dec 1; 
		mov 2; dec 1; 
	mov 2; pool; assert_zero 2; 
	mov 4; inc 1; 
	mov 5; loop; mov 4; loop; mov 4; dec 1; 
		mov 4; pool; assert_zero 4; 
		mov 2; inc 1; 
		mov 5; dec 1; 
	mov 5; pool; assert_zero 5; 
	mov 6; loop; mov 6; dec 1; 
	mov 6; pool; assert_zero 6; 
	mov 6; inc 1; 
	mov 4; loop; mov 5; inc 1; 
		mov 6; dec 1; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 1; loop; mov 3; inc 1; 
		mov 4; inc 1; 
		mov 1; dec 1; 
	mov 1; pool; assert_zero 1; 
	mov 4; loop; mov 1; inc 1; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 5; loop; mov 4; inc 1; 
		mov 5; dec 1; 
	mov 5; pool; assert_zero 5; 
	mov 3; dec 1; 
	mov 3; loop; mov 4; loop; mov 4; dec 1; 
		mov 4; pool; assert_zero 4; 
		mov 3; loop; mov 3; dec 1; 
		mov 3; pool; assert_zero 3; 
	mov 3; pool; assert_zero 3; 
	mov 5; inc 1; 
	mov 4; loop; mov 5; loop; mov 5; dec 1; 
		mov 5; pool; assert_zero 5; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 8; 
	mov 3; loop; mov 2; inc 6; 
		mov 1; inc 6; 
		mov 3; dec 1; 
	mov 3; pool; assert_zero 3; 
	mov 6; loop; mov 7; inc 1; 
		mov 8; inc 1; 
		mov 6; dec 1; 
	mov 6; pool; assert_zero 6; 
	mov 8; loop; mov 6; inc 1; 
		mov 8; dec 1; 
	mov 8; pool; assert_zero 8; 
	mov 7; loop; mov 2; output; 
		mov 7; dec 1; 
	mov 7; pool; assert_zero 7; 
	mov 1; output; 
	mov 3; loop; mov 3; dec 1; 
	mov 3; pool; assert_zero 3; 
	mov 4; loop; mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 4; inc 4; 
	mov 4; loop; mov 3; inc 8; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 4; 
	mov 4; loop; mov 3; inc 8; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 2; mov 3; output; 
	mov 4; inc 5; 
	mov 4; loop; mov 3; inc 9; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 3; inc 5; mov 3; output; 
	mov 3; output; 
	mov 3; dec 8; mov 3; output; 
	mov 3; dec 7; mov 3; output; 
	mov 5; loop; mov 7; inc 1; 
		mov 8; inc 1; 
		mov 5; dec 1; 
	mov 5; pool; assert_zero 5; 
	mov 8; loop; mov 5; inc 1; 
		mov 8; dec 1; 
	mov 8; pool; assert_zero 8; 
	mov 7; loop; mov 3; inc 14; mov 3; output; 
		mov 7; dec 1; 
	mov 7; pool; assert_zero 7; 
	mov 3; loop; mov 3; dec 1; 
	mov 3; pool; assert_zero 3; 
	mov 4; inc 4; 
	mov 4; loop; mov 3; inc 8; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 9; 
	mov 4; loop; mov 3; inc 9; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 2; mov 3; output; 
	mov 3; dec 9; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 6; 
	mov 4; loop; mov 3; inc 11; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 3; inc 3; mov 3; output; 
	mov 3; output; 
	mov 3; inc 13; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 2; mov 3; output; 
	mov 4; inc 9; 
	mov 4; loop; mov 3; inc 9; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 2; mov 3; output; 
	mov 3; dec 1; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 2; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; inc 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 4; mov 3; output; 
	mov 3; dec 12; mov 3; output; 
	mov 3; dec 3; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 1; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; inc 11; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 1; mov 3; output; 
	mov 4; inc 2; 
	mov 4; loop; mov 3; dec 11; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 3; inc 11; mov 3; output; 
	mov 3; output; 
	mov 4; inc 9; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 5; mov 3; output; 
	mov 3; dec 3; mov 3; output; 
	mov 6; loop; mov 7; inc 1; 
		mov 8; inc 1; 
		mov 6; dec 1; 
	mov 6; pool; assert_zero 6; 
	mov 8; loop; mov 6; inc 1; 
		mov 8; dec 1; 
	mov 8; pool; assert_zero 8; 
	mov 7; loop; mov 2; output; 
		mov 7; dec 1; 
	mov 7; pool; assert_zero 7; 
	mov 1; output; 
	mov 4; inc 4; 
	mov 4; loop; mov 3; inc 6; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 2; mov 3; output; 
	mov 4; inc 4; 
	mov 4; loop; mov 3; inc 8; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 2; mov 3; output; 
	mov 4; inc 5; 
	mov 4; loop; mov 3; inc 9; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 3; inc 5; mov 3; output; 
	mov 3; output; 
	mov 3; dec 8; mov 3; output; 
	mov 3; dec 7; mov 3; output; 
	mov 5; loop; mov 7; inc 1; 
		mov 8; inc 1; 
		mov 5; dec 1; 
	mov 5; pool; assert_zero 5; 
	mov 8; loop; mov 5; inc 1; 
		mov 8; dec 1; 
	mov 8; pool; assert_zero 8; 
	mov 7; loop; mov 3; inc 14; mov 3; output; 
		mov 7; dec 1; 
	mov 7; pool; assert_zero 7; 
	mov 3; loop; mov 3; dec 1; 
	mov 3; pool; assert_zero 3; 
	mov 4; inc 4; 
	mov 4; loop; mov 3; inc 8; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 9; 
	mov 4; loop; mov 3; inc 9; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 2; mov 3; output; 
	mov 3; dec 9; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 6; 
	mov 4; loop; mov 3; inc 11; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 3; inc 3; mov 3; output; 
	mov 3; output; 
	mov 3; inc 13; mov 3; output; 
	mov 4; inc 10; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 1; mov 3; output; 
	mov 3; dec 3; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; inc 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 4; mov 3; output; 
	mov 3; inc 13; mov 3; output; 
	mov 3; inc 10; mov 3; output; 
	mov 3; dec 6; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 1; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; inc 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 1; mov 3; output; 
	mov 3; dec 1; mov 3; output; 
	mov 3; dec 9; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 1; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; inc 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 2; mov 3; output; 
	mov 3; inc 11; mov 3; output; 
	mov 3; inc 8; mov 3; output; 
	mov 3; dec 9; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 2; mov 3; output; 
	mov 4; inc 5; 
	mov 4; loop; mov 3; inc 13; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 3; inc 13; mov 3; output; 
	mov 3; dec 10; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 2; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; inc 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 3; 
	mov 4; loop; mov 3; dec 5; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 3; 
	mov 4; loop; mov 3; inc 6; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 3; output; 
	mov 4; inc 9; 
	mov 4; loop; mov 3; dec 9; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 2; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; inc 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 3; mov 3; output; 
	mov 3; inc 11; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; dec 11; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 4; mov 3; output; 
	mov 4; inc 5; 
	mov 4; loop; mov 3; inc 13; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 3; 
	mov 4; loop; mov 3; inc 6; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 1; mov 3; output; 
	mov 3; dec 3; mov 3; output; 
	mov 3; inc 6; mov 3; output; 
	mov 3; dec 7; mov 3; output; 
	mov 3; dec 10; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; dec 11; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 1; mov 3; output; 
	mov 3; dec 3; mov 3; output; 
	mov 3; loop; mov 3; dec 1; 
	mov 3; pool; assert_zero 3; 
	mov 0; dec 1; 
	mov 1; loop; mov 1; dec 1; 
	mov 1; pool; assert_zero 1; 
	mov 2; loop; mov 2; dec 1; 
	mov 2; pool; assert_zero 2; 
	mov 0; loop; mov 1; inc 1; 
		mov 2; inc 1; 
		mov 0; dec 1; 
	mov 0; pool; assert_zero 0; 
	mov 2; loop; mov 0; inc 1; 
		mov 2; dec 1; 
	mov 2; pool; assert_zero 2; 
	mov 5; loop; mov 5; dec 1; 
	mov 5; pool; assert_zero 5; 
	mov 2; inc 9; 
	mov 1; loop; mov 4; inc 1; 
		mov 2; loop; mov 3; inc 1; 
			mov 4; loop; mov 4; dec 1; 
			mov 4; pool; assert_zero 4; 
			mov 2; dec 1; 
		mov 2; pool; assert_zero 2; 
		mov 3; loop; mov 2; inc 1; 
			mov 3; dec 1; 
		mov 3; pool; assert_zero 3; 
		mov 4; loop; mov 2; inc 10; 
			mov 5; inc 1; 
			mov 4; dec 1; 
		mov 4; pool; assert_zero 4; 
		mov 2; dec 1; 
		mov 1; dec 1; 
	mov 1; pool; assert_zero 1; 
	mov 1; inc 9; 
	mov 2; loop; mov 1; dec 1; 
		mov 2; dec 1; 
	mov 2; pool; assert_zero 2; 
	mov 4; inc 1; 
	mov 5; loop; mov 4; loop; mov 4; dec 1; 
		mov 4; pool; assert_zero 4; 
		mov 2; inc 1; 
		mov 5; dec 1; 
	mov 5; pool; assert_zero 5; 
	mov 6; loop; mov 6; dec 1; 
	mov 6; pool; assert_zero 6; 
	mov 6; inc 1; 
	mov 4; loop; mov 5; inc 1; 
		mov 6; dec 1; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 1; loop; mov 3; inc 1; 
		mov 4; inc 1; 
		mov 1; dec 1; 
	mov 1; pool; assert_zero 1; 
	mov 4; loop; mov 1; inc 1; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 5; loop; mov 4; inc 1; 
		mov 5; dec 1; 
	mov 5; pool; assert_zero 5; 
	mov 3; dec 1; 
	mov 3; loop; mov 4; loop; mov 4; dec 1; 
		mov 4; pool; assert_zero 4; 
		mov 3; loop; mov 3; dec 1; 
		mov 3; pool; assert_zero 3; 
	mov 3; pool; assert_zero 3; 
	mov 5; inc 1; 
	mov 4; loop; mov 5; loop; mov 5; dec 1; 
		mov 5; pool; assert_zero 5; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 8; 
	mov 3; loop; mov 2; inc 6; 
		mov 1; inc 6; 
		mov 3; dec 1; 
	mov 3; pool; assert_zero 3; 
	mov 6; loop; mov 7; inc 1; 
		mov 8; inc 1; 
		mov 6; dec 1; 
	mov 6; pool; assert_zero 6; 
	mov 8; loop; mov 6; inc 1; 
		mov 8; dec 1; 
	mov 8; pool; assert_zero 8; 
	mov 7; loop; mov 2; output; 
		mov 7; dec 1; 
	mov 7; pool; assert_zero 7; 
	mov 1; output; 
	mov 3; loop; mov 3; dec 1; 
	mov 3; pool; assert_zero 3; 
	mov 4; loop; mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 4; inc 4; 
	mov 4; loop; mov 3; inc 8; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 4; 
	mov 4; loop; mov 3; inc 8; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 2; mov 3; output; 
	mov 4; inc 5; 
	mov 4; loop; mov 3; inc 9; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 3; inc 5; mov 3; output; 
	mov 3; output; 
	mov 3; dec 8; mov 3; output; 
	mov 3; dec 7; mov 3; output; 
	mov 5; loop; mov 7; inc 1; 
		mov 8; inc 1; 
		mov 5; dec 1; 
	mov 5; pool; assert_zero 5; 
	mov 8; loop; mov 5; inc 1; 
		mov 8; dec 1; 
	mov 8; pool; assert_zero 8; 
	mov 7; loop; mov 3; inc 14; mov 3; output; 
		mov 7; dec 1; 
	mov 7; pool; assert_zero 7; 
	mov 3; loop; mov 3; dec 1; 
	mov 3; pool; assert_zero 3; 
	mov 4; inc 4; 
	mov 4; loop; mov 3; inc 8; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 9; 
	mov 4; loop; mov 3; inc 9; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 2; mov 3; output; 
	mov 3; dec 9; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 4; inc 6; 
	mov 4; loop; mov 3; inc 11; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 3; inc 3; mov 3; output; 
	mov 3; output; 
	mov 3; inc 13; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 2; mov 3; output; 
	mov 4; inc 9; 
	mov 4; loop; mov 3; inc 9; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 2; mov 3; output; 
	mov 3; dec 1; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 2; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; inc 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 4; mov 3; output; 
	mov 3; dec 12; mov 3; output; 
	mov 3; dec 3; mov 3; output; 
	mov 4; inc 7; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; inc 1; mov 3; output; 
	mov 4; inc 8; 
	mov 4; loop; mov 3; inc 11; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 1; mov 3; output; 
	mov 4; inc 2; 
	mov 4; loop; mov 3; dec 11; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; output; 
	mov 3; inc 11; mov 3; output; 
	mov 3; output; 
	mov 4; inc 9; 
	mov 4; loop; mov 3; dec 10; 
		mov 4; dec 1; 
	mov 4; pool; assert_zero 4; 
	mov 3; dec 5; mov 3; output; 
	mov 3; dec 3; mov 3; output; 
	mov 3; inc 3; mov 3; output; 
	mov 3; dec 3; mov 3; output; 
	mov 3; loop; mov 3; dec 1; 
	mov 3; pool; assert_zero 3; 
mov 0; pool; assert_zero 0; 
